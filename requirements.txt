# ==============================================================
# PPOCRv4 Mobile OCR 訓練環境 — 雲端 GPU Server / Colab 用
# ==============================================================
# 安裝方式：
#   pip install -r requirements.txt
#
# 注意：paddlepaddle-gpu 需配合 CUDA 版本
#   CUDA 11.8: pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu118/
#   CUDA 12.6: pip install paddlepaddle-gpu==3.0.0 -i https://www.paddlepaddle.org.cn/packages/stable/cu126/
#   CPU 版本:  pip install paddlepaddle==3.0.0
# ==============================================================

# --- PaddlePaddle 核心（GPU 版本，根據 CUDA 版本選擇安裝方式）---
# paddlepaddle-gpu==3.0.0  # 用上面的指令安裝，不要從這裡裝

# --- PaddleOCR ---
paddleocr>=3.0.0

# --- 模型匯出 / ONNX 轉換 ---
paddle2onnx>=1.3.0
onnxruntime>=1.17.0         # CPU 推論驗證用
# onnxruntime-gpu>=1.17.0  # 如需 GPU 推論取消註解

# --- 影像處理 ---
opencv-python>=4.6.0
opencv-contrib-python>=4.6.0
Pillow>=9.0.0
imgaug>=0.4.0

# --- 資料處理 ---
numpy>=1.21.0
scipy>=1.7.0
scikit-image>=0.19.0

# --- 視覺化 / 日誌 ---
visualdl>=2.5.0             # PaddlePaddle 的 TensorBoard 替代
tqdm>=4.64.0
PyYAML>=6.0

# --- 其他依賴 ---
requests>=2.25.0
shapely>=1.7.0
pyclipper>=1.3.0
lmdb>=1.3.0                # rec 訓練資料格式
rapidfuzz>=2.0.0
